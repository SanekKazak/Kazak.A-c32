spring:

  application:
    name: hometask47-il-control-hotel

  main:
    allow-bean-definition-overriding: true

  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
server:
  port: 8080
